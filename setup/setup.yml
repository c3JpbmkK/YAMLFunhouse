---
- name: Setup vsftpd on control
  import_playbook: playbooks/setup-localhost.yml
  vars:
    site_directory: /home/ansible/kubernetes/setup

- name: Setup repositories on nodes
  import_playbook: playbooks/setup-managed-nodes.yml
  vars:
    site_directory: /home/ansible/kubernetes/setup
